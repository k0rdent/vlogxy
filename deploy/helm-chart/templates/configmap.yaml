apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "victorialogs-aggregator.fullname" . }}-config
  labels:
    {{- include "victorialogs-aggregator.labels" . | nindent 4 }}
data:
  {{ .Values.config.fileName }}: |
    {{- toYaml .Values.config.data | nindent 4 }}
