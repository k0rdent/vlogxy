apiVersion: v1
kind: Service
metadata:
  name: {{ include "vlogxy.fullname" . }}
  labels:
    {{- include "vlogxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vlogxy.selectorLabels" . | nindent 4 }}
